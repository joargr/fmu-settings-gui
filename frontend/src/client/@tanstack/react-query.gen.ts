// This file is auto-generated by @hey-api/openapi-ts

import { type DefaultError, queryOptions, type UseMutationOptions } from '@tanstack/react-query';
import type { AxiosError } from 'axios';

import { client } from '../client.gen';
import { healthHealthCheck, healthV1HealthCheck, matchPostMatch, type Options, projectDeleteProjectSession, projectGetCache, projectGetCacheDiff, projectGetCacheRevision, projectGetChangelog, projectGetGlobalConfigStatus, projectGetLockStatus, projectGetMappings, projectGetProject, projectGetRestoreCheck, projectGetRmsProjects, projectGetSumoAssets, projectPatchAccess, projectPatchCacheMaxRevisions, projectPatchMasterdata, projectPatchModel, projectPatchRms, projectPatchRmsCoordinateSystem, projectPatchRmsStratigraphicFramework, projectPatchRmsWells, projectPostCacheRestore, projectPostGlobalConfig, projectPostInitProject, projectPostLockAcquire, projectPostLockRefresh, projectPostLockRelease, projectPostMappingsExportRmsSimulatorRenamingTable, projectPostMappingsImportRmsEclipseCsv, projectPostProject, projectPostRestore, projectPostValidateMasterdataSmda, projectPutMappings, rmsDeleteRmsProject, rmsGetCoordinateSystem, rmsGetHorizons, rmsGetWells, rmsGetZones, rmsPostRmsProject, sessionGetRestoreCheck, sessionGetSession, sessionPatchAccessToken, sessionPostRestore, sessionPostSession, smdaGetHealth, smdaPostField, smdaPostMasterdata, smdaPostStratUnits, smdaPostWellHeaders, userGetUser, userPatchApiKey } from '../sdk.gen';
import type { HealthHealthCheckData, HealthHealthCheckResponse, HealthV1HealthCheckData, HealthV1HealthCheckError, HealthV1HealthCheckResponse, MatchPostMatchData, MatchPostMatchResponse, ProjectDeleteProjectSessionData, ProjectDeleteProjectSessionError, ProjectDeleteProjectSessionResponse, ProjectGetCacheData, ProjectGetCacheDiffData, ProjectGetCacheDiffResponse, ProjectGetCacheError, ProjectGetCacheResponse, ProjectGetCacheRevisionData, ProjectGetCacheRevisionResponse, ProjectGetChangelogData, ProjectGetChangelogResponse, ProjectGetGlobalConfigStatusData, ProjectGetGlobalConfigStatusResponse, ProjectGetLockStatusData, ProjectGetLockStatusError, ProjectGetLockStatusResponse, ProjectGetMappingsData, ProjectGetMappingsResponse, ProjectGetProjectData, ProjectGetProjectError, ProjectGetProjectResponse, ProjectGetRestoreCheckData, ProjectGetRestoreCheckError, ProjectGetRestoreCheckResponse, ProjectGetRmsProjectsData, ProjectGetRmsProjectsError, ProjectGetRmsProjectsResponse, ProjectGetSumoAssetsData, ProjectGetSumoAssetsResponse, ProjectPatchAccessData, ProjectPatchAccessError, ProjectPatchAccessResponse, ProjectPatchCacheMaxRevisionsData, ProjectPatchCacheMaxRevisionsError, ProjectPatchCacheMaxRevisionsResponse, ProjectPatchMasterdataData, ProjectPatchMasterdataError, ProjectPatchMasterdataResponse, ProjectPatchModelData, ProjectPatchModelError, ProjectPatchModelResponse, ProjectPatchRmsCoordinateSystemData, ProjectPatchRmsCoordinateSystemResponse, ProjectPatchRmsData, ProjectPatchRmsError, ProjectPatchRmsResponse, ProjectPatchRmsStratigraphicFrameworkData, ProjectPatchRmsStratigraphicFrameworkResponse, ProjectPatchRmsWellsData, ProjectPatchRmsWellsResponse, ProjectPostCacheRestoreData, ProjectPostCacheRestoreResponse, ProjectPostGlobalConfigData, ProjectPostGlobalConfigResponse, ProjectPostInitProjectData, ProjectPostInitProjectResponse, ProjectPostLockAcquireData, ProjectPostLockAcquireError, ProjectPostLockAcquireResponse, ProjectPostLockRefreshData, ProjectPostLockRefreshError, ProjectPostLockRefreshResponse, ProjectPostLockReleaseData, ProjectPostLockReleaseError, ProjectPostLockReleaseResponse, ProjectPostMappingsExportRmsSimulatorRenamingTableData, ProjectPostMappingsExportRmsSimulatorRenamingTableResponse, ProjectPostMappingsImportRmsEclipseCsvData, ProjectPostMappingsImportRmsEclipseCsvResponse, ProjectPostProjectData, ProjectPostProjectError, ProjectPostProjectResponse, ProjectPostRestoreData, ProjectPostRestoreError, ProjectPostRestoreResponse, ProjectPostValidateMasterdataSmdaData, ProjectPostValidateMasterdataSmdaResponse, ProjectPutMappingsData, ProjectPutMappingsResponse, RmsDeleteRmsProjectData, RmsDeleteRmsProjectError, RmsDeleteRmsProjectResponse, RmsGetCoordinateSystemData, RmsGetCoordinateSystemError, RmsGetCoordinateSystemResponse, RmsGetHorizonsData, RmsGetHorizonsError, RmsGetHorizonsResponse, RmsGetWellsData, RmsGetWellsError, RmsGetWellsResponse, RmsGetZonesData, RmsGetZonesError, RmsGetZonesResponse, RmsPostRmsProjectData, RmsPostRmsProjectResponse, SessionGetRestoreCheckData, SessionGetRestoreCheckError, SessionGetRestoreCheckResponse, SessionGetSessionData, SessionGetSessionError, SessionGetSessionResponse, SessionPatchAccessTokenData, SessionPatchAccessTokenError, SessionPatchAccessTokenResponse, SessionPostRestoreData, SessionPostRestoreError, SessionPostRestoreResponse, SessionPostSessionData, SessionPostSessionError, SessionPostSessionResponse, SmdaGetHealthData, SmdaGetHealthError, SmdaGetHealthResponse, SmdaPostFieldData, SmdaPostFieldError, SmdaPostFieldResponse, SmdaPostMasterdataData, SmdaPostMasterdataResponse, SmdaPostStratUnitsData, SmdaPostStratUnitsResponse, SmdaPostWellHeadersData, SmdaPostWellHeadersResponse, UserGetUserData, UserGetUserError, UserGetUserResponse, UserPatchApiKeyData, UserPatchApiKeyError, UserPatchApiKeyResponse } from '../types.gen';

/**
 * Removes a project .fmu directory from a session
 *
 * This route simply removes (closes) a project .fmu directory from a session.
 * This has no other side effects on the session.
 */
export const projectDeleteProjectSessionMutation = (options?: Partial<Options<ProjectDeleteProjectSessionData>>): UseMutationOptions<ProjectDeleteProjectSessionResponse, AxiosError<ProjectDeleteProjectSessionError>, Options<ProjectDeleteProjectSessionData>> => {
    const mutationOptions: UseMutationOptions<ProjectDeleteProjectSessionResponse, AxiosError<ProjectDeleteProjectSessionError>, Options<ProjectDeleteProjectSessionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectDeleteProjectSession({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'baseURL' | 'body' | 'headers' | 'path' | 'query'> & {
        _id: string;
        _infinite?: boolean;
        tags?: ReadonlyArray<string>;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, infinite?: boolean, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseURL: options?.baseURL || (options?.client ?? client).getConfig().baseURL } as QueryKey<TOptions>[0];
    if (infinite) {
        params._infinite = infinite;
    }
    if (tags) {
        params.tags = tags;
    }
    if (options?.body) {
        params.body = options.body;
    }
    if (options?.headers) {
        params.headers = options.headers;
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query) {
        params.query = options.query;
    }
    return [params];
};

export const projectGetProjectQueryKey = (options?: Options<ProjectGetProjectData>) => createQueryKey('projectGetProject', options);

/**
 * Returns the paths and configuration of the nearest project .fmu directory
 *
 * If a project is not already attached to the session id it will be
 * attached after a call to this route. If one is already attached this
 * route will return data for the project .fmu directory again.
 */
export const projectGetProjectOptions = (options?: Options<ProjectGetProjectData>) => queryOptions<ProjectGetProjectResponse, AxiosError<ProjectGetProjectError>, ProjectGetProjectResponse, ReturnType<typeof projectGetProjectQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetProject({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetProjectQueryKey(options)
});

/**
 * Returns the path and configuration of the project .fmu directory at 'path'
 *
 * Used for when a user selects a project .fmu directory in a directory not
 * found above the user's current working directory. Will overwrite the
 * project .fmu directory attached to a session if one exists. If not, it is
 * added to the session.
 */
export const projectPostProjectMutation = (options?: Partial<Options<ProjectPostProjectData>>): UseMutationOptions<ProjectPostProjectResponse, AxiosError<ProjectPostProjectError>, Options<ProjectPostProjectData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostProjectResponse, AxiosError<ProjectPostProjectError>, Options<ProjectPostProjectData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostProject({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const projectGetSumoAssetsQueryKey = (options?: Options<ProjectGetSumoAssetsData>) => createQueryKey('projectGetSumoAssets', options);

/**
 * Returns a list of Sumo assets.
 *
 * Returns a list of the assets that have been onboarded to
 * the Sumo platform.
 */
export const projectGetSumoAssetsOptions = (options?: Options<ProjectGetSumoAssetsData>) => queryOptions<ProjectGetSumoAssetsResponse, AxiosError<DefaultError>, ProjectGetSumoAssetsResponse, ReturnType<typeof projectGetSumoAssetsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetSumoAssets({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetSumoAssetsQueryKey(options)
});

export const projectGetGlobalConfigStatusQueryKey = (options?: Options<ProjectGetGlobalConfigStatusData>) => createQueryKey('projectGetGlobalConfigStatus', options);

/**
 * Checks if a valid global config exists at the default location.
 *
 * Checks the global config at the default project location. If the global config
 * does not validate, or is not found, a failed status code is returned.
 */
export const projectGetGlobalConfigStatusOptions = (options?: Options<ProjectGetGlobalConfigStatusData>) => queryOptions<ProjectGetGlobalConfigStatusResponse, AxiosError<DefaultError>, ProjectGetGlobalConfigStatusResponse, ReturnType<typeof projectGetGlobalConfigStatusQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetGlobalConfigStatus({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetGlobalConfigStatusQueryKey(options)
});

/**
 * Initializes a project .fmu directory at 'path' and returns its paths and configuration
 *
 * If a project .fmu directory is already attached to the session, this will
 * switch to use the newly created .fmu directory.
 */
export const projectPostInitProjectMutation = (options?: Partial<Options<ProjectPostInitProjectData>>): UseMutationOptions<ProjectPostInitProjectResponse, AxiosError<DefaultError>, Options<ProjectPostInitProjectData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostInitProjectResponse, AxiosError<DefaultError>, Options<ProjectPostInitProjectData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostInitProject({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Loads the global config into the project masterdata.
 *
 * Loads the global config into the project masterdata. If the global config does
 * not validate, or is not found, a failed status code is returned. The endpoint
 * takes an optional parameter, `path` as input: This should be given as a relative
 * path, relative to the project root. If provided, the global config is searched
 * for at this path. If not, the default project path will be used.
 */
export const projectPostGlobalConfigMutation = (options?: Partial<Options<ProjectPostGlobalConfigData>>): UseMutationOptions<ProjectPostGlobalConfigResponse, AxiosError<DefaultError>, Options<ProjectPostGlobalConfigData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostGlobalConfigResponse, AxiosError<DefaultError>, Options<ProjectPostGlobalConfigData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostGlobalConfig({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Attempts to acquire the project lock for editing
 *
 * Tries to upgrade the project session from read-only to editable by acquiring
 * the project lock. If the lock cannot be acquired the project remains read-only
 * and the last lock acquire error is recorded in the session.
 */
export const projectPostLockAcquireMutation = (options?: Partial<Options<ProjectPostLockAcquireData>>): UseMutationOptions<ProjectPostLockAcquireResponse, AxiosError<ProjectPostLockAcquireError>, Options<ProjectPostLockAcquireData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostLockAcquireResponse, AxiosError<ProjectPostLockAcquireError>, Options<ProjectPostLockAcquireData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostLockAcquire({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Attempts to release the project lock
 *
 * Tries to release the project lock for the current session.
 * If the lock is not currently held, the route returns an informational message.
 */
export const projectPostLockReleaseMutation = (options?: Partial<Options<ProjectPostLockReleaseData>>): UseMutationOptions<ProjectPostLockReleaseResponse, AxiosError<ProjectPostLockReleaseError>, Options<ProjectPostLockReleaseData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostLockReleaseResponse, AxiosError<ProjectPostLockReleaseError>, Options<ProjectPostLockReleaseData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostLockRelease({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Refreshes the project lock timeout
 *
 * Explicitly refreshes the project lock timeout if the current session
 * holds the lock. This should be called when the user actively indicates they
 * want to continue editing (e.g., pressing an 'Edit' button in the GUI).
 *
 * Returns a message indicating whether the lock was successfully refreshed.
 */
export const projectPostLockRefreshMutation = (options?: Partial<Options<ProjectPostLockRefreshData>>): UseMutationOptions<ProjectPostLockRefreshResponse, AxiosError<ProjectPostLockRefreshError>, Options<ProjectPostLockRefreshData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostLockRefreshResponse, AxiosError<ProjectPostLockRefreshError>, Options<ProjectPostLockRefreshData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostLockRefresh({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const projectGetLockStatusQueryKey = (options?: Options<ProjectGetLockStatusData>) => createQueryKey('projectGetLockStatus', options);

/**
 * Returns the lock status and lock file contents
 *
 * Returns information about the project lock including whether the current
 * session holds the lock and the contents of the lock file if it exists.
 * This is useful for debugging lock conflicts and showing users who has
 * the project locked.
 */
export const projectGetLockStatusOptions = (options?: Options<ProjectGetLockStatusData>) => queryOptions<ProjectGetLockStatusResponse, AxiosError<ProjectGetLockStatusError>, ProjectGetLockStatusResponse, ReturnType<typeof projectGetLockStatusQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetLockStatus({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetLockStatusQueryKey(options)
});

/**
 * Saves SMDA masterdata to the project .fmu directory
 *
 * Saves masterdata from SMDA to the project .fmu directory.
 * If existing masterdata is present, it will be updated with the new masterdata.
 */
export const projectPatchMasterdataMutation = (options?: Partial<Options<ProjectPatchMasterdataData>>): UseMutationOptions<ProjectPatchMasterdataResponse, AxiosError<ProjectPatchMasterdataError>, Options<ProjectPatchMasterdataData>> => {
    const mutationOptions: UseMutationOptions<ProjectPatchMasterdataResponse, AxiosError<ProjectPatchMasterdataError>, Options<ProjectPatchMasterdataData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPatchMasterdata({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Validates project masterdata against SMDA
 *
 * Validates the saved project SMDA masterdata against current SMDA data.
 * If validation succeeds, validation metadata is updated in the project
 * config. If validation fails, no validation metadata is written.
 */
export const projectPostValidateMasterdataSmdaMutation = (options?: Partial<Options<ProjectPostValidateMasterdataSmdaData>>): UseMutationOptions<ProjectPostValidateMasterdataSmdaResponse, AxiosError<DefaultError>, Options<ProjectPostValidateMasterdataSmdaData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostValidateMasterdataSmdaResponse, AxiosError<DefaultError>, Options<ProjectPostValidateMasterdataSmdaData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostValidateMasterdataSmda({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Saves model data to the project .fmu directory
 *
 * Saves model data to the project .fmu directory.
 * If existing model data is present, it will be replaced by the new
 * model data.
 */
export const projectPatchModelMutation = (options?: Partial<Options<ProjectPatchModelData>>): UseMutationOptions<ProjectPatchModelResponse, AxiosError<ProjectPatchModelError>, Options<ProjectPatchModelData>> => {
    const mutationOptions: UseMutationOptions<ProjectPatchModelResponse, AxiosError<ProjectPatchModelError>, Options<ProjectPatchModelData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPatchModel({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Saves access data to the project .fmu directory
 *
 * Saves access data to the project .fmu directory.
 * If existing access data is present, it will be replaced by the new
 * access data.
 */
export const projectPatchAccessMutation = (options?: Partial<Options<ProjectPatchAccessData>>): UseMutationOptions<ProjectPatchAccessResponse, AxiosError<ProjectPatchAccessError>, Options<ProjectPatchAccessData>> => {
    const mutationOptions: UseMutationOptions<ProjectPatchAccessResponse, AxiosError<ProjectPatchAccessError>, Options<ProjectPatchAccessData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPatchAccess({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Saves cache max revisions to the project .fmu directory
 *
 * Saves the maximum number of cache revisions to keep per resource in the
 * project .fmu configuration.
 */
export const projectPatchCacheMaxRevisionsMutation = (options?: Partial<Options<ProjectPatchCacheMaxRevisionsData>>): UseMutationOptions<ProjectPatchCacheMaxRevisionsResponse, AxiosError<ProjectPatchCacheMaxRevisionsError>, Options<ProjectPatchCacheMaxRevisionsData>> => {
    const mutationOptions: UseMutationOptions<ProjectPatchCacheMaxRevisionsResponse, AxiosError<ProjectPatchCacheMaxRevisionsError>, Options<ProjectPatchCacheMaxRevisionsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPatchCacheMaxRevisions({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const projectGetRmsProjectsQueryKey = (options?: Options<ProjectGetRmsProjectsData>) => createQueryKey('projectGetRmsProjects', options);

/**
 * Gets the paths of RMS projects in this project directory
 *
 * Returns a list of paths to RMS projects found in the current project directory.
 */
export const projectGetRmsProjectsOptions = (options?: Options<ProjectGetRmsProjectsData>) => queryOptions<ProjectGetRmsProjectsResponse, AxiosError<ProjectGetRmsProjectsError>, ProjectGetRmsProjectsResponse, ReturnType<typeof projectGetRmsProjectsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetRmsProjects({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetRmsProjectsQueryKey(options)
});

/**
 * Saves the RMS project path and version in the project .fmu directory
 *
 * Saves the RMS project path and version to the project .fmu directory.
 * The RMS version is set automatically based on the provided RMS project path.
 * If existing RMS project path and version are present, they will be
 * replaced by the new RMS project path and version.
 */
export const projectPatchRmsMutation = (options?: Partial<Options<ProjectPatchRmsData>>): UseMutationOptions<ProjectPatchRmsResponse, AxiosError<ProjectPatchRmsError>, Options<ProjectPatchRmsData>> => {
    const mutationOptions: UseMutationOptions<ProjectPatchRmsResponse, AxiosError<ProjectPatchRmsError>, Options<ProjectPatchRmsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPatchRms({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Saves the RMS coordinate system in the project .fmu directory
 *
 * Saves the RMS coordinate system to the project .fmu directory.
 * Requires that the RMS project path has been set first via PATCH /project/rms.
 * If an existing coordinate system is present, it will be replaced.
 */
export const projectPatchRmsCoordinateSystemMutation = (options?: Partial<Options<ProjectPatchRmsCoordinateSystemData>>): UseMutationOptions<ProjectPatchRmsCoordinateSystemResponse, AxiosError<DefaultError>, Options<ProjectPatchRmsCoordinateSystemData>> => {
    const mutationOptions: UseMutationOptions<ProjectPatchRmsCoordinateSystemResponse, AxiosError<DefaultError>, Options<ProjectPatchRmsCoordinateSystemData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPatchRmsCoordinateSystem({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Saves the RMS stratigraphic framework in the project .fmu directory
 *
 * Saves the RMS stratigraphic framework (zones and horizons) to the project
 * .fmu directory. Requires that the RMS project path has been set first via
 * PATCH /project/rms. If existing zones or horizons are present, they will
 * be replaced.
 */
export const projectPatchRmsStratigraphicFrameworkMutation = (options?: Partial<Options<ProjectPatchRmsStratigraphicFrameworkData>>): UseMutationOptions<ProjectPatchRmsStratigraphicFrameworkResponse, AxiosError<DefaultError>, Options<ProjectPatchRmsStratigraphicFrameworkData>> => {
    const mutationOptions: UseMutationOptions<ProjectPatchRmsStratigraphicFrameworkResponse, AxiosError<DefaultError>, Options<ProjectPatchRmsStratigraphicFrameworkData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPatchRmsStratigraphicFramework({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Saves the RMS wells in the project .fmu directory
 *
 * Saves the RMS wells to the project .fmu directory.
 * Requires that the RMS project path has been set first via PATCH /project/rms.
 * If existing wells are present, they will be replaced.
 */
export const projectPatchRmsWellsMutation = (options?: Partial<Options<ProjectPatchRmsWellsData>>): UseMutationOptions<ProjectPatchRmsWellsResponse, AxiosError<DefaultError>, Options<ProjectPatchRmsWellsData>> => {
    const mutationOptions: UseMutationOptions<ProjectPatchRmsWellsResponse, AxiosError<DefaultError>, Options<ProjectPatchRmsWellsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPatchRmsWells({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const projectGetCacheQueryKey = (options: Options<ProjectGetCacheData>) => createQueryKey('projectGetCache', options);

/**
 * List cache revisions
 *
 * Returns a list of revision filenames for the resource specified by the
 * `resource` query parameter. The filenames are used with
 * GET /cache/{revision_id}.
 */
export const projectGetCacheOptions = (options: Options<ProjectGetCacheData>) => queryOptions<ProjectGetCacheResponse, AxiosError<ProjectGetCacheError>, ProjectGetCacheResponse, ReturnType<typeof projectGetCacheQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetCache({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetCacheQueryKey(options)
});

export const projectGetCacheRevisionQueryKey = (options: Options<ProjectGetCacheRevisionData>) => createQueryKey('projectGetCacheRevision', options);

/**
 * Get cache revision content
 *
 * Retrieve the content of a specific cache revision.
 *
 * The revision_id should be a filename from the list returned by GET /cache
 * (e.g., 20260112T143045.123456Z-a1b2c3d4.json).
 *
 * The `resource` query parameter selects which resource cache to read.
 *
 * Returns the parsed JSON content from the cached file in the `data` field.
 */
export const projectGetCacheRevisionOptions = (options: Options<ProjectGetCacheRevisionData>) => queryOptions<ProjectGetCacheRevisionResponse, AxiosError<DefaultError>, ProjectGetCacheRevisionResponse, ReturnType<typeof projectGetCacheRevisionQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetCacheRevision({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetCacheRevisionQueryKey(options)
});

export const projectGetCacheDiffQueryKey = (options: Options<ProjectGetCacheDiffData>) => createQueryKey('projectGetCacheDiff', options);

/**
 * Get diff between current resource and cache revision
 *
 * Compare a resource file in the current project with a cached revision.
 *
 * The `resource` query parameter selects which resource to diff.
 * The response is a list of changes keyed by `field_path`.
 */
export const projectGetCacheDiffOptions = (options: Options<ProjectGetCacheDiffData>) => queryOptions<ProjectGetCacheDiffResponse, AxiosError<DefaultError>, ProjectGetCacheDiffResponse, ReturnType<typeof projectGetCacheDiffQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetCacheDiff({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetCacheDiffQueryKey(options)
});

/**
 * Restore a resource from a cache revision
 *
 * Restore a resource from a cache revision (overwrites current resource).
 *
 * The current resource state is cached before overwriting (when present).
 *
 * The `resource` query parameter selects which resource to restore.
 *
 * **Example flow:**
 *
 * 1. Current resource is in state A
 * 2. Call restore with a revision from state B
 * 3. Your current state A is cached (when present)
 * 4. Resource is now in state B
 * 5. To undo: restore from the newly created cache entry (your state A backup)
 */
export const projectPostCacheRestoreMutation = (options?: Partial<Options<ProjectPostCacheRestoreData>>): UseMutationOptions<ProjectPostCacheRestoreResponse, AxiosError<DefaultError>, Options<ProjectPostCacheRestoreData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostCacheRestoreResponse, AxiosError<DefaultError>, Options<ProjectPostCacheRestoreData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostCacheRestore({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const projectGetRestoreCheckQueryKey = (options?: Options<ProjectGetRestoreCheckData>) => createQueryKey('projectGetRestoreCheck', options);

/**
 * Checks which missing project .fmu resources can be restored
 *
 * Lists recoverable missing project .fmu content from in-memory state.
 */
export const projectGetRestoreCheckOptions = (options?: Options<ProjectGetRestoreCheckData>) => queryOptions<ProjectGetRestoreCheckResponse, AxiosError<ProjectGetRestoreCheckError>, ProjectGetRestoreCheckResponse, ReturnType<typeof projectGetRestoreCheckQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetRestoreCheck({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetRestoreCheckQueryKey(options)
});

/**
 * Restores missing project .fmu resources for the current session
 *
 * Attempts to recover missing project .fmu content from in-memory state.
 */
export const projectPostRestoreMutation = (options?: Partial<Options<ProjectPostRestoreData>>): UseMutationOptions<ProjectPostRestoreResponse, AxiosError<ProjectPostRestoreError>, Options<ProjectPostRestoreData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostRestoreResponse, AxiosError<ProjectPostRestoreError>, Options<ProjectPostRestoreData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostRestore({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const projectGetMappingsQueryKey = (options: Options<ProjectGetMappingsData>) => createQueryKey('projectGetMappings', options);

/**
 * Returns internal mappings for a specific mapping type and source system.
 *
 * Retrieves internal mappings for the specified mapping_type and source_system
 * from the project's .fmu directory.
 *
 * Example: GET /project/mappings/stratigraphy/rms returns the
 * internal stratigraphy mappings whose source system is RMS.
 */
export const projectGetMappingsOptions = (options: Options<ProjectGetMappingsData>) => queryOptions<ProjectGetMappingsResponse, AxiosError<DefaultError>, ProjectGetMappingsResponse, ReturnType<typeof projectGetMappingsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetMappings({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetMappingsQueryKey(options)
});

/**
 * Updates internal mappings for a specific mapping type and source system
 *
 * Replaces internal mappings for the specified mapping_type and source system in
 * the project's .fmu directory.
 *
 * The request body should contain the internal mappings collection for the
 * specified source system.
 *
 * Example: PUT /project/mappings/stratigraphy/rms replaces the stored
 * internal stratigraphy mappings whose source system is RMS.
 */
export const projectPutMappingsMutation = (options?: Partial<Options<ProjectPutMappingsData>>): UseMutationOptions<ProjectPutMappingsResponse, AxiosError<DefaultError>, Options<ProjectPutMappingsData>> => {
    const mutationOptions: UseMutationOptions<ProjectPutMappingsResponse, AxiosError<DefaultError>, Options<ProjectPutMappingsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPutMappings({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Imports RMS-to-simulator wellbore mappings from rms_eclipse.csv.
 *
 * Reads an rms_eclipse.csv-format file from the project and returns the
 * imported RMS-to-simulator mappings in the internal wellbore mapping format.
 * The path is relative to the project root. If no path is provided, the default
 * path relative to the project root is
 * rms/input/well_modelling/well_info/rms_eclipse.csv.
 */
export const projectPostMappingsImportRmsEclipseCsvMutation = (options?: Partial<Options<ProjectPostMappingsImportRmsEclipseCsvData>>): UseMutationOptions<ProjectPostMappingsImportRmsEclipseCsvResponse, AxiosError<DefaultError>, Options<ProjectPostMappingsImportRmsEclipseCsvData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostMappingsImportRmsEclipseCsvResponse, AxiosError<DefaultError>, Options<ProjectPostMappingsImportRmsEclipseCsvData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostMappingsImportRmsEclipseCsv({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Exports RMS-to-simulator wellbore mappings to a renaming table.
 *
 * Reads RMS-to-simulator wellbore mappings from mappings.json and writes them
 * to an rms_simulator renaming-table file. The path is relative to the project
 * root. If no path is provided, the default path relative to the project root is
 * rms/input/well_modelling/well_info/rms_simulator.renaming_table.
 */
export const projectPostMappingsExportRmsSimulatorRenamingTableMutation = (options?: Partial<Options<ProjectPostMappingsExportRmsSimulatorRenamingTableData>>): UseMutationOptions<ProjectPostMappingsExportRmsSimulatorRenamingTableResponse, AxiosError<DefaultError>, Options<ProjectPostMappingsExportRmsSimulatorRenamingTableData>> => {
    const mutationOptions: UseMutationOptions<ProjectPostMappingsExportRmsSimulatorRenamingTableResponse, AxiosError<DefaultError>, Options<ProjectPostMappingsExportRmsSimulatorRenamingTableData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await projectPostMappingsExportRmsSimulatorRenamingTable({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const projectGetChangelogQueryKey = (options?: Options<ProjectGetChangelogData>) => createQueryKey('projectGetChangelog', options);

/**
 * Returns changelog for the project.
 *
 * Retrieves changelog from the project's .fmu directory.
 */
export const projectGetChangelogOptions = (options?: Options<ProjectGetChangelogData>) => queryOptions<ProjectGetChangelogResponse, AxiosError<DefaultError>, ProjectGetChangelogResponse, ReturnType<typeof projectGetChangelogQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await projectGetChangelog({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: projectGetChangelogQueryKey(options)
});

export const userGetUserQueryKey = (options?: Options<UserGetUserData>) => createQueryKey('userGetUser', options);

/**
 * Returns the user .fmu configuration
 *
 * The user configuration can store API subscription keys or tokens. These are
 * obfuscated as '**********' when returned.
 */
export const userGetUserOptions = (options?: Options<UserGetUserData>) => queryOptions<UserGetUserResponse, AxiosError<UserGetUserError>, UserGetUserResponse, ReturnType<typeof userGetUserQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await userGetUser({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: userGetUserQueryKey(options)
});

/**
 * Saves an API key/token to the user .fmu configuration
 *
 * Currently only known API's can be saved to the user .fmu configuration.
 * Arbitrary API key-value pairs cannot be saved. The currently known APIs are:
 *
 * smda_subscription
 */
export const userPatchApiKeyMutation = (options?: Partial<Options<UserPatchApiKeyData>>): UseMutationOptions<UserPatchApiKeyResponse, AxiosError<UserPatchApiKeyError>, Options<UserPatchApiKeyData>> => {
    const mutationOptions: UseMutationOptions<UserPatchApiKeyResponse, AxiosError<UserPatchApiKeyError>, Options<UserPatchApiKeyData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await userPatchApiKey({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const sessionGetSessionQueryKey = (options?: Options<SessionGetSessionData>) => createQueryKey('sessionGetSession', options);

/**
 * Fetches the current session state
 *
 * Retrieves the latest session metadata.
 */
export const sessionGetSessionOptions = (options?: Options<SessionGetSessionData>) => queryOptions<SessionGetSessionResponse, AxiosError<SessionGetSessionError>, SessionGetSessionResponse, ReturnType<typeof sessionGetSessionQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await sessionGetSession({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: sessionGetSessionQueryKey(options)
});

/**
 * Creates a new user session.
 *
 * When creating a session the application will ensure that the user
 * .fmu directory exists by creating it if it does not.
 *
 * If a valid session already exists when POSTing to this route, the
 * existing session will be renewed with a new expiry date and a new
 * session cookie value.
 *
 * After creating the session, the application will attempt to find the
 * nearest project .fmu directory above the current working directory and
 * add it to the session if found. If not found, no project will be
 * associated. Renewing an existing session keeps its current state.
 *
 * The session cookie set by this route is required for all other
 * routes. Sessions are not persisted when the API is shut down.
 */
export const sessionPostSessionMutation = (options?: Partial<Options<SessionPostSessionData>>): UseMutationOptions<SessionPostSessionResponse, AxiosError<SessionPostSessionError>, Options<SessionPostSessionData>> => {
    const mutationOptions: UseMutationOptions<SessionPostSessionResponse, AxiosError<SessionPostSessionError>, Options<SessionPostSessionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await sessionPostSession({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Adds a known access token to the session
 *
 * This route should be used to add a scoped access token to the current
 * session. The token applied via this route is typically a dependency for
 * other routes.
 */
export const sessionPatchAccessTokenMutation = (options?: Partial<Options<SessionPatchAccessTokenData>>): UseMutationOptions<SessionPatchAccessTokenResponse, AxiosError<SessionPatchAccessTokenError>, Options<SessionPatchAccessTokenData>> => {
    const mutationOptions: UseMutationOptions<SessionPatchAccessTokenResponse, AxiosError<SessionPatchAccessTokenError>, Options<SessionPatchAccessTokenData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await sessionPatchAccessToken({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const sessionGetRestoreCheckQueryKey = (options?: Options<SessionGetRestoreCheckData>) => createQueryKey('sessionGetRestoreCheck', options);

/**
 * Checks which missing user .fmu resources can be restored
 *
 * Lists recoverable missing user .fmu content from in-memory state.
 */
export const sessionGetRestoreCheckOptions = (options?: Options<SessionGetRestoreCheckData>) => queryOptions<SessionGetRestoreCheckResponse, AxiosError<SessionGetRestoreCheckError>, SessionGetRestoreCheckResponse, ReturnType<typeof sessionGetRestoreCheckQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await sessionGetRestoreCheck({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: sessionGetRestoreCheckQueryKey(options)
});

/**
 * Restores missing user .fmu resources for the current session
 *
 * Attempts to recover missing user .fmu content from in-memory state.
 */
export const sessionPostRestoreMutation = (options?: Partial<Options<SessionPostRestoreData>>): UseMutationOptions<SessionPostRestoreResponse, AxiosError<SessionPostRestoreError>, Options<SessionPostRestoreData>> => {
    const mutationOptions: UseMutationOptions<SessionPostRestoreResponse, AxiosError<SessionPostRestoreError>, Options<SessionPostRestoreData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await sessionPostRestore({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Close the RMS project in the session
 *
 * Close the RMS project that is currently open in the session.
 *
 * This removes the RMS project reference from the session. The project
 * should be closed when it is no longer needed to free up resources.
 */
export const rmsDeleteRmsProjectMutation = (options?: Partial<Options<RmsDeleteRmsProjectData>>): UseMutationOptions<RmsDeleteRmsProjectResponse, AxiosError<RmsDeleteRmsProjectError>, Options<RmsDeleteRmsProjectData>> => {
    const mutationOptions: UseMutationOptions<RmsDeleteRmsProjectResponse, AxiosError<RmsDeleteRmsProjectError>, Options<RmsDeleteRmsProjectData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await rmsDeleteRmsProject({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Open an RMS project and store it in the session
 *
 * Open an RMS project and store it in the session.
 *
 * The RMS project path must be configured in the project's .fmu config file.
 * Once opened, the project remains open in the session until explicitly closed
 * or the session expires. This allows for efficient repeated access without
 * reopening the project each time.
 *
 * The endpoint takes an optional parameter, `rms_version`, as input. This can be
 * used to specify which version of RMS API that should be used when opening
 * the RMS project.
 */
export const rmsPostRmsProjectMutation = (options?: Partial<Options<RmsPostRmsProjectData>>): UseMutationOptions<RmsPostRmsProjectResponse, AxiosError<DefaultError>, Options<RmsPostRmsProjectData>> => {
    const mutationOptions: UseMutationOptions<RmsPostRmsProjectResponse, AxiosError<DefaultError>, Options<RmsPostRmsProjectData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await rmsPostRmsProject({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const rmsGetZonesQueryKey = (options?: Options<RmsGetZonesData>) => createQueryKey('rmsGetZones', options);

/**
 * Get the zones from the open RMS project
 *
 * Retrieve the zones from the currently open RMS project.
 *
 * This endpoint requires an RMS project to be open in the session.
 * Use the POST / endpoint first to open an RMS project.
 */
export const rmsGetZonesOptions = (options?: Options<RmsGetZonesData>) => queryOptions<RmsGetZonesResponse, AxiosError<RmsGetZonesError>, RmsGetZonesResponse, ReturnType<typeof rmsGetZonesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await rmsGetZones({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: rmsGetZonesQueryKey(options)
});

export const rmsGetHorizonsQueryKey = (options?: Options<RmsGetHorizonsData>) => createQueryKey('rmsGetHorizons', options);

/**
 * Get all horizons from the open RMS project
 *
 * Retrieve all horizons from the currently open RMS project.
 *
 * This endpoint requires an RMS project to be open in the session.
 * Use the POST / endpoint first to open an RMS project.
 */
export const rmsGetHorizonsOptions = (options?: Options<RmsGetHorizonsData>) => queryOptions<RmsGetHorizonsResponse, AxiosError<RmsGetHorizonsError>, RmsGetHorizonsResponse, ReturnType<typeof rmsGetHorizonsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await rmsGetHorizons({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: rmsGetHorizonsQueryKey(options)
});

export const rmsGetWellsQueryKey = (options?: Options<RmsGetWellsData>) => createQueryKey('rmsGetWells', options);

/**
 * Get all wells from the open RMS project
 *
 * Retrieve all wells from the currently open RMS project.
 *
 * This endpoint requires an RMS project to be open in the session.
 * Use the POST / endpoint first to open an RMS project.
 */
export const rmsGetWellsOptions = (options?: Options<RmsGetWellsData>) => queryOptions<RmsGetWellsResponse, AxiosError<RmsGetWellsError>, RmsGetWellsResponse, ReturnType<typeof rmsGetWellsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await rmsGetWells({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: rmsGetWellsQueryKey(options)
});

export const rmsGetCoordinateSystemQueryKey = (options?: Options<RmsGetCoordinateSystemData>) => createQueryKey('rmsGetCoordinateSystem', options);

/**
 * Get the project coordinate system from the open RMS project
 *
 * Retrieve the project coordinate system from the currently open RMS project.
 *
 * This endpoint requires an RMS project to be open in the session.
 * Use the POST / endpoint first to open an RMS project.
 */
export const rmsGetCoordinateSystemOptions = (options?: Options<RmsGetCoordinateSystemData>) => queryOptions<RmsGetCoordinateSystemResponse, AxiosError<RmsGetCoordinateSystemError>, RmsGetCoordinateSystemResponse, ReturnType<typeof rmsGetCoordinateSystemQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await rmsGetCoordinateSystem({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: rmsGetCoordinateSystemQueryKey(options)
});

export const smdaGetHealthQueryKey = (options?: Options<SmdaGetHealthData>) => createQueryKey('smdaGetHealth', options);

/**
 * Checks whether or not the current session is capable of querying SMDA
 *
 * A route to check whether the client is capable of querying SMDA APIs
 * with their current session. The requirements for querying the SMDA API via
 * this API are:
 *
 * 1. A valid session
 * 2. An SMDA subscription key in the user's .fmu API key configuration
 * 3. A valid SMDA access token scoped to SMDA's user_impersonation scope
 *
 * A successful response from this route indicates that all other routes on the
 * SMDA router can be used.
 */
export const smdaGetHealthOptions = (options?: Options<SmdaGetHealthData>) => queryOptions<SmdaGetHealthResponse, AxiosError<SmdaGetHealthError>, SmdaGetHealthResponse, ReturnType<typeof smdaGetHealthQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await smdaGetHealth({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: smdaGetHealthQueryKey(options)
});

export const smdaPostFieldQueryKey = (options: Options<SmdaPostFieldData>) => createQueryKey('smdaPostField', options);

/**
 * Searches for a field identifier in SMDA
 *
 * A route to search SMDA for a field (asset) by its named identifier.
 *
 * This endpoint applies a projection to the SMDA query so that only the relevant
 * data is returned: an identifier known by SMDA, its corresponding UUID, and the
 * main country the field belongs to, not a list of countries where the field
 * exists. The UUID should be used by other endpoints required the collection of
 * data by a field, i.e. this route is a dependency for most other routes.
 *
 * The number of results (hits) and number of pages those results span over is also
 * returned in the result. This endpoint does not implement pagination. The
 * current expectation is that a user would refine their search rather than page
 * through different results.
 */
export const smdaPostFieldOptions = (options: Options<SmdaPostFieldData>) => queryOptions<SmdaPostFieldResponse, AxiosError<SmdaPostFieldError>, SmdaPostFieldResponse, ReturnType<typeof smdaPostFieldQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await smdaPostField({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: smdaPostFieldQueryKey(options)
});

/**
 * Searches for a field identifier in SMDA
 *
 * A route to search SMDA for a field (asset) by its named identifier.
 *
 * This endpoint applies a projection to the SMDA query so that only the relevant
 * data is returned: an identifier known by SMDA, its corresponding UUID, and the
 * main country the field belongs to, not a list of countries where the field
 * exists. The UUID should be used by other endpoints required the collection of
 * data by a field, i.e. this route is a dependency for most other routes.
 *
 * The number of results (hits) and number of pages those results span over is also
 * returned in the result. This endpoint does not implement pagination. The
 * current expectation is that a user would refine their search rather than page
 * through different results.
 */
export const smdaPostFieldMutation = (options?: Partial<Options<SmdaPostFieldData>>): UseMutationOptions<SmdaPostFieldResponse, AxiosError<SmdaPostFieldError>, Options<SmdaPostFieldData>> => {
    const mutationOptions: UseMutationOptions<SmdaPostFieldResponse, AxiosError<SmdaPostFieldError>, Options<SmdaPostFieldData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await smdaPostField({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const smdaPostMasterdataQueryKey = (options: Options<SmdaPostMasterdataData>) => createQueryKey('smdaPostMasterdata', options);

/**
 * Retrieves masterdata for fields to be confirmed in the GUI
 *
 * A route to gather prospective SMDA masterdata relevant to FMU.
 *
 * This route receives a list of valid field names and returns masterdata that
 * pertains to them. The field names should be valid as returned from the
 * `smda/field` route. Callers may also provide a field UUID to disambiguate
 * fields that share the same identifier. If the first field does not include
 * a UUID, the endpoint searches for all provided field identifiers. If the
 * first field includes a UUID, the endpoint looks up only that field, even if
 * more fields are included in the request.
 *
 * The data returned from this endpoint is meant to be confirmed by the user who
 * may need to do some additional selection or pruning based upon the model they
 * are working from.
 *
 * One example of this is changing the coordinate system. A model may use a
 * coordinate system different from the one set as the field's default coordinate
 * system in SMDA. To match the way this works on SMDA, every coordinate system
 * known to SMDA is returned.
 *
 * This endpoint does multiple calls to the SMDA API, any of which may possibly
 * fail. In any of these calls fails incomplete data will _not_ be returned; that
 * is, a successful code with partial data will not be returned.
 */
export const smdaPostMasterdataOptions = (options: Options<SmdaPostMasterdataData>) => queryOptions<SmdaPostMasterdataResponse, AxiosError<DefaultError>, SmdaPostMasterdataResponse, ReturnType<typeof smdaPostMasterdataQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await smdaPostMasterdata({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: smdaPostMasterdataQueryKey(options)
});

/**
 * Retrieves masterdata for fields to be confirmed in the GUI
 *
 * A route to gather prospective SMDA masterdata relevant to FMU.
 *
 * This route receives a list of valid field names and returns masterdata that
 * pertains to them. The field names should be valid as returned from the
 * `smda/field` route. Callers may also provide a field UUID to disambiguate
 * fields that share the same identifier. If the first field does not include
 * a UUID, the endpoint searches for all provided field identifiers. If the
 * first field includes a UUID, the endpoint looks up only that field, even if
 * more fields are included in the request.
 *
 * The data returned from this endpoint is meant to be confirmed by the user who
 * may need to do some additional selection or pruning based upon the model they
 * are working from.
 *
 * One example of this is changing the coordinate system. A model may use a
 * coordinate system different from the one set as the field's default coordinate
 * system in SMDA. To match the way this works on SMDA, every coordinate system
 * known to SMDA is returned.
 *
 * This endpoint does multiple calls to the SMDA API, any of which may possibly
 * fail. In any of these calls fails incomplete data will _not_ be returned; that
 * is, a successful code with partial data will not be returned.
 */
export const smdaPostMasterdataMutation = (options?: Partial<Options<SmdaPostMasterdataData>>): UseMutationOptions<SmdaPostMasterdataResponse, AxiosError<DefaultError>, Options<SmdaPostMasterdataData>> => {
    const mutationOptions: UseMutationOptions<SmdaPostMasterdataResponse, AxiosError<DefaultError>, Options<SmdaPostMasterdataData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await smdaPostMasterdata({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const smdaPostStratUnitsQueryKey = (options: Options<SmdaPostStratUnitsData>) => createQueryKey('smdaPostStratUnits', options);

/**
 * Retrieves stratigraphic units for a stratigraphic column
 *
 * A route to gather stratigraphic units from SMDA for a specified
 * stratigraphic column.
 *
 * This route receives a valid stratigraphic column identifier
 * and returns stratigraphic units associated with it. The identifier
 * should be obtained from the `stratigraphic_columns` field in the
 * `smda/masterdata` response.
 */
export const smdaPostStratUnitsOptions = (options: Options<SmdaPostStratUnitsData>) => queryOptions<SmdaPostStratUnitsResponse, AxiosError<DefaultError>, SmdaPostStratUnitsResponse, ReturnType<typeof smdaPostStratUnitsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await smdaPostStratUnits({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: smdaPostStratUnitsQueryKey(options)
});

/**
 * Retrieves stratigraphic units for a stratigraphic column
 *
 * A route to gather stratigraphic units from SMDA for a specified
 * stratigraphic column.
 *
 * This route receives a valid stratigraphic column identifier
 * and returns stratigraphic units associated with it. The identifier
 * should be obtained from the `stratigraphic_columns` field in the
 * `smda/masterdata` response.
 */
export const smdaPostStratUnitsMutation = (options?: Partial<Options<SmdaPostStratUnitsData>>): UseMutationOptions<SmdaPostStratUnitsResponse, AxiosError<DefaultError>, Options<SmdaPostStratUnitsData>> => {
    const mutationOptions: UseMutationOptions<SmdaPostStratUnitsResponse, AxiosError<DefaultError>, Options<SmdaPostStratUnitsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await smdaPostStratUnits({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const smdaPostWellHeadersQueryKey = (options: Options<SmdaPostWellHeadersData>) => createQueryKey('smdaPostWellHeaders', options);

/**
 * Retrieves well headers for a field
 *
 * A route to gather well header data from SMDA for a specified field.
 *
 * This route receives a valid field identifier and returns the well header
 * fields needed to identify and map SMDA wellbores against model well names.
 */
export const smdaPostWellHeadersOptions = (options: Options<SmdaPostWellHeadersData>) => queryOptions<SmdaPostWellHeadersResponse, AxiosError<DefaultError>, SmdaPostWellHeadersResponse, ReturnType<typeof smdaPostWellHeadersQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await smdaPostWellHeaders({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: smdaPostWellHeadersQueryKey(options)
});

/**
 * Retrieves well headers for a field
 *
 * A route to gather well header data from SMDA for a specified field.
 *
 * This route receives a valid field identifier and returns the well header
 * fields needed to identify and map SMDA wellbores against model well names.
 */
export const smdaPostWellHeadersMutation = (options?: Partial<Options<SmdaPostWellHeadersData>>): UseMutationOptions<SmdaPostWellHeadersResponse, AxiosError<DefaultError>, Options<SmdaPostWellHeadersData>> => {
    const mutationOptions: UseMutationOptions<SmdaPostWellHeadersResponse, AxiosError<DefaultError>, Options<SmdaPostWellHeadersData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await smdaPostWellHeaders({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const matchPostMatchQueryKey = (options: Options<MatchPostMatchData>) => createQueryKey('matchPostMatch', options);

/**
 * Match source names to target names
 *
 * Match source names to target names using strict name similarity.
 *
 * The endpoint is a pure matching utility. Callers provide both the
 * source names and target names, and can optionally provide string
 * replacement rules to apply before matching.
 *
 * Names are normalized before matching by lowercasing, replacing
 * underscores, dots, dashes, and slashes with spaces, collapsing
 * whitespace, and applying optional replacement rules. Replacement
 * rules match whole normalized token sequences only, so a rule like
 * `Fm -> Formation` changes `Tarbert Fm` to `Tarbert Formation`,
 * while `Top -> ""` leaves `Stop Viking` unchanged.
 *
 * The response contains one result per source. Each result contains up
 * to three target matches, ordered from highest to lowest score.
 *
 * Confidence levels:
 * - 'high': score > 80
 * - 'medium': score 50-80
 * - 'low': score < 50
 */
export const matchPostMatchOptions = (options: Options<MatchPostMatchData>) => queryOptions<MatchPostMatchResponse, AxiosError<DefaultError>, MatchPostMatchResponse, ReturnType<typeof matchPostMatchQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await matchPostMatch({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: matchPostMatchQueryKey(options)
});

/**
 * Match source names to target names
 *
 * Match source names to target names using strict name similarity.
 *
 * The endpoint is a pure matching utility. Callers provide both the
 * source names and target names, and can optionally provide string
 * replacement rules to apply before matching.
 *
 * Names are normalized before matching by lowercasing, replacing
 * underscores, dots, dashes, and slashes with spaces, collapsing
 * whitespace, and applying optional replacement rules. Replacement
 * rules match whole normalized token sequences only, so a rule like
 * `Fm -> Formation` changes `Tarbert Fm` to `Tarbert Formation`,
 * while `Top -> ""` leaves `Stop Viking` unchanged.
 *
 * The response contains one result per source. Each result contains up
 * to three target matches, ordered from highest to lowest score.
 *
 * Confidence levels:
 * - 'high': score > 80
 * - 'medium': score 50-80
 * - 'low': score < 50
 */
export const matchPostMatchMutation = (options?: Partial<Options<MatchPostMatchData>>): UseMutationOptions<MatchPostMatchResponse, AxiosError<DefaultError>, Options<MatchPostMatchData>> => {
    const mutationOptions: UseMutationOptions<MatchPostMatchResponse, AxiosError<DefaultError>, Options<MatchPostMatchData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await matchPostMatch({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const healthV1HealthCheckQueryKey = (options?: Options<HealthV1HealthCheckData>) => createQueryKey('healthV1HealthCheck', options);

/**
 * A health check on the /v1 routes.
 *
 * This route requires a valid session to return 200 OK. it can used to check if the user has a valid session.
 */
export const healthV1HealthCheckOptions = (options?: Options<HealthV1HealthCheckData>) => queryOptions<HealthV1HealthCheckResponse, AxiosError<HealthV1HealthCheckError>, HealthV1HealthCheckResponse, ReturnType<typeof healthV1HealthCheckQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await healthV1HealthCheck({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: healthV1HealthCheckQueryKey(options)
});

export const healthHealthCheckQueryKey = (options?: Options<HealthHealthCheckData>) => createQueryKey('healthHealthCheck', options);

/**
 * A health check on the application
 *
 * This route requires no form of authentication or authorization. It can be used to check if the application is running and responsive.
 */
export const healthHealthCheckOptions = (options?: Options<HealthHealthCheckData>) => queryOptions<HealthHealthCheckResponse, AxiosError<DefaultError>, HealthHealthCheckResponse, ReturnType<typeof healthHealthCheckQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await healthHealthCheck({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: healthHealthCheckQueryKey(options)
});
