// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { HealthHealthCheckData, HealthHealthCheckResponses, HealthV1HealthCheckData, HealthV1HealthCheckErrors, HealthV1HealthCheckResponses, MatchPostMatchData, MatchPostMatchErrors, MatchPostMatchResponses, ProjectDeleteProjectSessionData, ProjectDeleteProjectSessionErrors, ProjectDeleteProjectSessionResponses, ProjectGetCacheData, ProjectGetCacheDiffData, ProjectGetCacheDiffErrors, ProjectGetCacheDiffResponses, ProjectGetCacheErrors, ProjectGetCacheResponses, ProjectGetCacheRevisionData, ProjectGetCacheRevisionErrors, ProjectGetCacheRevisionResponses, ProjectGetChangelogData, ProjectGetChangelogErrors, ProjectGetChangelogResponses, ProjectGetGlobalConfigStatusData, ProjectGetGlobalConfigStatusErrors, ProjectGetGlobalConfigStatusResponses, ProjectGetLockStatusData, ProjectGetLockStatusErrors, ProjectGetLockStatusResponses, ProjectGetMappingsData, ProjectGetMappingsErrors, ProjectGetMappingsResponses, ProjectGetProjectData, ProjectGetProjectErrors, ProjectGetProjectResponses, ProjectGetRestoreCheckData, ProjectGetRestoreCheckErrors, ProjectGetRestoreCheckResponses, ProjectGetRmsProjectsData, ProjectGetRmsProjectsErrors, ProjectGetRmsProjectsResponses, ProjectGetSumoAssetsData, ProjectGetSumoAssetsErrors, ProjectGetSumoAssetsResponses, ProjectPatchAccessData, ProjectPatchAccessErrors, ProjectPatchAccessResponses, ProjectPatchCacheMaxRevisionsData, ProjectPatchCacheMaxRevisionsErrors, ProjectPatchCacheMaxRevisionsResponses, ProjectPatchMasterdataData, ProjectPatchMasterdataErrors, ProjectPatchMasterdataResponses, ProjectPatchModelData, ProjectPatchModelErrors, ProjectPatchModelResponses, ProjectPatchRmsCoordinateSystemData, ProjectPatchRmsCoordinateSystemErrors, ProjectPatchRmsCoordinateSystemResponses, ProjectPatchRmsData, ProjectPatchRmsErrors, ProjectPatchRmsResponses, ProjectPatchRmsStratigraphicFrameworkData, ProjectPatchRmsStratigraphicFrameworkErrors, ProjectPatchRmsStratigraphicFrameworkResponses, ProjectPatchRmsWellsData, ProjectPatchRmsWellsErrors, ProjectPatchRmsWellsResponses, ProjectPostCacheRestoreData, ProjectPostCacheRestoreErrors, ProjectPostCacheRestoreResponses, ProjectPostGlobalConfigData, ProjectPostGlobalConfigErrors, ProjectPostGlobalConfigResponses, ProjectPostInitProjectData, ProjectPostInitProjectErrors, ProjectPostInitProjectResponses, ProjectPostLockAcquireData, ProjectPostLockAcquireErrors, ProjectPostLockAcquireResponses, ProjectPostLockRefreshData, ProjectPostLockRefreshErrors, ProjectPostLockRefreshResponses, ProjectPostLockReleaseData, ProjectPostLockReleaseErrors, ProjectPostLockReleaseResponses, ProjectPostMappingsExportRmsSimulatorRenamingTableData, ProjectPostMappingsExportRmsSimulatorRenamingTableErrors, ProjectPostMappingsExportRmsSimulatorRenamingTableResponses, ProjectPostMappingsImportRmsEclipseCsvData, ProjectPostMappingsImportRmsEclipseCsvErrors, ProjectPostMappingsImportRmsEclipseCsvResponses, ProjectPostProjectData, ProjectPostProjectErrors, ProjectPostProjectResponses, ProjectPostRestoreData, ProjectPostRestoreErrors, ProjectPostRestoreResponses, ProjectPostValidateMasterdataSmdaData, ProjectPostValidateMasterdataSmdaErrors, ProjectPostValidateMasterdataSmdaResponses, ProjectPutMappingsData, ProjectPutMappingsErrors, ProjectPutMappingsResponses, RmsDeleteRmsProjectData, RmsDeleteRmsProjectErrors, RmsDeleteRmsProjectResponses, RmsGetCoordinateSystemData, RmsGetCoordinateSystemErrors, RmsGetCoordinateSystemResponses, RmsGetHorizonsData, RmsGetHorizonsErrors, RmsGetHorizonsResponses, RmsGetWellsData, RmsGetWellsErrors, RmsGetWellsResponses, RmsGetZonesData, RmsGetZonesErrors, RmsGetZonesResponses, RmsPostRmsProjectData, RmsPostRmsProjectErrors, RmsPostRmsProjectResponses, SessionGetRestoreCheckData, SessionGetRestoreCheckErrors, SessionGetRestoreCheckResponses, SessionGetSessionData, SessionGetSessionErrors, SessionGetSessionResponses, SessionPatchAccessTokenData, SessionPatchAccessTokenErrors, SessionPatchAccessTokenResponses, SessionPostRestoreData, SessionPostRestoreErrors, SessionPostRestoreResponses, SessionPostSessionData, SessionPostSessionErrors, SessionPostSessionResponses, SmdaGetHealthData, SmdaGetHealthErrors, SmdaGetHealthResponses, SmdaPostFieldData, SmdaPostFieldErrors, SmdaPostFieldResponses, SmdaPostMasterdataData, SmdaPostMasterdataErrors, SmdaPostMasterdataResponses, SmdaPostStratUnitsData, SmdaPostStratUnitsErrors, SmdaPostStratUnitsResponses, SmdaPostWellHeadersData, SmdaPostWellHeadersErrors, SmdaPostWellHeadersResponses, UserGetUserData, UserGetUserErrors, UserGetUserResponses, UserPatchApiKeyData, UserPatchApiKeyErrors, UserPatchApiKeyResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Removes a project .fmu directory from a session
 *
 * This route simply removes (closes) a project .fmu directory from a session.
 * This has no other side effects on the session.
 */
export const projectDeleteProjectSession = <ThrowOnError extends boolean = false>(options?: Options<ProjectDeleteProjectSessionData, ThrowOnError>): RequestResult<ProjectDeleteProjectSessionResponses, ProjectDeleteProjectSessionErrors, ThrowOnError> => (options?.client ?? client).delete<ProjectDeleteProjectSessionResponses, ProjectDeleteProjectSessionErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/',
    ...options
});

/**
 * Returns the paths and configuration of the nearest project .fmu directory
 *
 * If a project is not already attached to the session id it will be
 * attached after a call to this route. If one is already attached this
 * route will return data for the project .fmu directory again.
 */
export const projectGetProject = <ThrowOnError extends boolean = false>(options?: Options<ProjectGetProjectData, ThrowOnError>): RequestResult<ProjectGetProjectResponses, ProjectGetProjectErrors, ThrowOnError> => (options?.client ?? client).get<ProjectGetProjectResponses, ProjectGetProjectErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/',
    ...options
});

/**
 * Returns the path and configuration of the project .fmu directory at 'path'
 *
 * Used for when a user selects a project .fmu directory in a directory not
 * found above the user's current working directory. Will overwrite the
 * project .fmu directory attached to a session if one exists. If not, it is
 * added to the session.
 */
export const projectPostProject = <ThrowOnError extends boolean = false>(options: Options<ProjectPostProjectData, ThrowOnError>): RequestResult<ProjectPostProjectResponses, ProjectPostProjectErrors, ThrowOnError> => (options.client ?? client).post<ProjectPostProjectResponses, ProjectPostProjectErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Returns a list of Sumo assets.
 *
 * Returns a list of the assets that have been onboarded to
 * the Sumo platform.
 */
export const projectGetSumoAssets = <ThrowOnError extends boolean = false>(options?: Options<ProjectGetSumoAssetsData, ThrowOnError>): RequestResult<ProjectGetSumoAssetsResponses, ProjectGetSumoAssetsErrors, ThrowOnError> => (options?.client ?? client).get<ProjectGetSumoAssetsResponses, ProjectGetSumoAssetsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/sumo_assets',
    ...options
});

/**
 * Checks if a valid global config exists at the default location.
 *
 * Checks the global config at the default project location. If the global config
 * does not validate, or is not found, a failed status code is returned.
 */
export const projectGetGlobalConfigStatus = <ThrowOnError extends boolean = false>(options?: Options<ProjectGetGlobalConfigStatusData, ThrowOnError>): RequestResult<ProjectGetGlobalConfigStatusResponses, ProjectGetGlobalConfigStatusErrors, ThrowOnError> => (options?.client ?? client).get<ProjectGetGlobalConfigStatusResponses, ProjectGetGlobalConfigStatusErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/global_config_status',
    ...options
});

/**
 * Initializes a project .fmu directory at 'path' and returns its paths and configuration
 *
 * If a project .fmu directory is already attached to the session, this will
 * switch to use the newly created .fmu directory.
 */
export const projectPostInitProject = <ThrowOnError extends boolean = false>(options: Options<ProjectPostInitProjectData, ThrowOnError>): RequestResult<ProjectPostInitProjectResponses, ProjectPostInitProjectErrors, ThrowOnError> => (options.client ?? client).post<ProjectPostInitProjectResponses, ProjectPostInitProjectErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/init',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Loads the global config into the project masterdata.
 *
 * Loads the global config into the project masterdata. If the global config does
 * not validate, or is not found, a failed status code is returned. The endpoint
 * takes an optional parameter, `path` as input: This should be given as a relative
 * path, relative to the project root. If provided, the global config is searched
 * for at this path. If not, the default project path will be used.
 */
export const projectPostGlobalConfig = <ThrowOnError extends boolean = false>(options?: Options<ProjectPostGlobalConfigData, ThrowOnError>): RequestResult<ProjectPostGlobalConfigResponses, ProjectPostGlobalConfigErrors, ThrowOnError> => (options?.client ?? client).post<ProjectPostGlobalConfigResponses, ProjectPostGlobalConfigErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/global_config',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Attempts to acquire the project lock for editing
 *
 * Tries to upgrade the project session from read-only to editable by acquiring
 * the project lock. If the lock cannot be acquired the project remains read-only
 * and the last lock acquire error is recorded in the session.
 */
export const projectPostLockAcquire = <ThrowOnError extends boolean = false>(options?: Options<ProjectPostLockAcquireData, ThrowOnError>): RequestResult<ProjectPostLockAcquireResponses, ProjectPostLockAcquireErrors, ThrowOnError> => (options?.client ?? client).post<ProjectPostLockAcquireResponses, ProjectPostLockAcquireErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/lock_acquire',
    ...options
});

/**
 * Attempts to release the project lock
 *
 * Tries to release the project lock for the current session.
 * If the lock is not currently held, the route returns an informational message.
 */
export const projectPostLockRelease = <ThrowOnError extends boolean = false>(options?: Options<ProjectPostLockReleaseData, ThrowOnError>): RequestResult<ProjectPostLockReleaseResponses, ProjectPostLockReleaseErrors, ThrowOnError> => (options?.client ?? client).post<ProjectPostLockReleaseResponses, ProjectPostLockReleaseErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/lock_release',
    ...options
});

/**
 * Refreshes the project lock timeout
 *
 * Explicitly refreshes the project lock timeout if the current session
 * holds the lock. This should be called when the user actively indicates they
 * want to continue editing (e.g., pressing an 'Edit' button in the GUI).
 *
 * Returns a message indicating whether the lock was successfully refreshed.
 */
export const projectPostLockRefresh = <ThrowOnError extends boolean = false>(options?: Options<ProjectPostLockRefreshData, ThrowOnError>): RequestResult<ProjectPostLockRefreshResponses, ProjectPostLockRefreshErrors, ThrowOnError> => (options?.client ?? client).post<ProjectPostLockRefreshResponses, ProjectPostLockRefreshErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/lock_refresh',
    ...options
});

/**
 * Returns the lock status and lock file contents
 *
 * Returns information about the project lock including whether the current
 * session holds the lock and the contents of the lock file if it exists.
 * This is useful for debugging lock conflicts and showing users who has
 * the project locked.
 */
export const projectGetLockStatus = <ThrowOnError extends boolean = false>(options?: Options<ProjectGetLockStatusData, ThrowOnError>): RequestResult<ProjectGetLockStatusResponses, ProjectGetLockStatusErrors, ThrowOnError> => (options?.client ?? client).get<ProjectGetLockStatusResponses, ProjectGetLockStatusErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/lock_status',
    ...options
});

/**
 * Saves SMDA masterdata to the project .fmu directory
 *
 * Saves masterdata from SMDA to the project .fmu directory.
 * If existing masterdata is present, it will be updated with the new masterdata.
 */
export const projectPatchMasterdata = <ThrowOnError extends boolean = false>(options: Options<ProjectPatchMasterdataData, ThrowOnError>): RequestResult<ProjectPatchMasterdataResponses, ProjectPatchMasterdataErrors, ThrowOnError> => (options.client ?? client).patch<ProjectPatchMasterdataResponses, ProjectPatchMasterdataErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/masterdata',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Validates project masterdata against SMDA
 *
 * Validates the saved project SMDA masterdata against current SMDA data.
 * If validation succeeds, validation metadata is updated in the project
 * config. If validation fails, no validation metadata is written.
 */
export const projectPostValidateMasterdataSmda = <ThrowOnError extends boolean = false>(options?: Options<ProjectPostValidateMasterdataSmdaData, ThrowOnError>): RequestResult<ProjectPostValidateMasterdataSmdaResponses, ProjectPostValidateMasterdataSmdaErrors, ThrowOnError> => (options?.client ?? client).post<ProjectPostValidateMasterdataSmdaResponses, ProjectPostValidateMasterdataSmdaErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/validate/masterdata/smda',
    ...options
});

/**
 * Saves model data to the project .fmu directory
 *
 * Saves model data to the project .fmu directory.
 * If existing model data is present, it will be replaced by the new
 * model data.
 */
export const projectPatchModel = <ThrowOnError extends boolean = false>(options: Options<ProjectPatchModelData, ThrowOnError>): RequestResult<ProjectPatchModelResponses, ProjectPatchModelErrors, ThrowOnError> => (options.client ?? client).patch<ProjectPatchModelResponses, ProjectPatchModelErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/model',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Saves access data to the project .fmu directory
 *
 * Saves access data to the project .fmu directory.
 * If existing access data is present, it will be replaced by the new
 * access data.
 */
export const projectPatchAccess = <ThrowOnError extends boolean = false>(options: Options<ProjectPatchAccessData, ThrowOnError>): RequestResult<ProjectPatchAccessResponses, ProjectPatchAccessErrors, ThrowOnError> => (options.client ?? client).patch<ProjectPatchAccessResponses, ProjectPatchAccessErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/access',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Saves cache max revisions to the project .fmu directory
 *
 * Saves the maximum number of cache revisions to keep per resource in the
 * project .fmu configuration.
 */
export const projectPatchCacheMaxRevisions = <ThrowOnError extends boolean = false>(options: Options<ProjectPatchCacheMaxRevisionsData, ThrowOnError>): RequestResult<ProjectPatchCacheMaxRevisionsResponses, ProjectPatchCacheMaxRevisionsErrors, ThrowOnError> => (options.client ?? client).patch<ProjectPatchCacheMaxRevisionsResponses, ProjectPatchCacheMaxRevisionsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/cache_max_revisions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Gets the paths of RMS projects in this project directory
 *
 * Returns a list of paths to RMS projects found in the current project directory.
 */
export const projectGetRmsProjects = <ThrowOnError extends boolean = false>(options?: Options<ProjectGetRmsProjectsData, ThrowOnError>): RequestResult<ProjectGetRmsProjectsResponses, ProjectGetRmsProjectsErrors, ThrowOnError> => (options?.client ?? client).get<ProjectGetRmsProjectsResponses, ProjectGetRmsProjectsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/rms_projects',
    ...options
});

/**
 * Saves the RMS project path and version in the project .fmu directory
 *
 * Saves the RMS project path and version to the project .fmu directory.
 * The RMS version is set automatically based on the provided RMS project path.
 * If existing RMS project path and version are present, they will be
 * replaced by the new RMS project path and version.
 */
export const projectPatchRms = <ThrowOnError extends boolean = false>(options: Options<ProjectPatchRmsData, ThrowOnError>): RequestResult<ProjectPatchRmsResponses, ProjectPatchRmsErrors, ThrowOnError> => (options.client ?? client).patch<ProjectPatchRmsResponses, ProjectPatchRmsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/rms',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Saves the RMS coordinate system in the project .fmu directory
 *
 * Saves the RMS coordinate system to the project .fmu directory.
 * Requires that the RMS project path has been set first via PATCH /project/rms.
 * If an existing coordinate system is present, it will be replaced.
 */
export const projectPatchRmsCoordinateSystem = <ThrowOnError extends boolean = false>(options: Options<ProjectPatchRmsCoordinateSystemData, ThrowOnError>): RequestResult<ProjectPatchRmsCoordinateSystemResponses, ProjectPatchRmsCoordinateSystemErrors, ThrowOnError> => (options.client ?? client).patch<ProjectPatchRmsCoordinateSystemResponses, ProjectPatchRmsCoordinateSystemErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/rms/coordinate_system',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Saves the RMS stratigraphic framework in the project .fmu directory
 *
 * Saves the RMS stratigraphic framework (zones and horizons) to the project
 * .fmu directory. Requires that the RMS project path has been set first via
 * PATCH /project/rms. If existing zones or horizons are present, they will
 * be replaced.
 */
export const projectPatchRmsStratigraphicFramework = <ThrowOnError extends boolean = false>(options: Options<ProjectPatchRmsStratigraphicFrameworkData, ThrowOnError>): RequestResult<ProjectPatchRmsStratigraphicFrameworkResponses, ProjectPatchRmsStratigraphicFrameworkErrors, ThrowOnError> => (options.client ?? client).patch<ProjectPatchRmsStratigraphicFrameworkResponses, ProjectPatchRmsStratigraphicFrameworkErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/rms/stratigraphic_framework',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Saves the RMS wells in the project .fmu directory
 *
 * Saves the RMS wells to the project .fmu directory.
 * Requires that the RMS project path has been set first via PATCH /project/rms.
 * If existing wells are present, they will be replaced.
 */
export const projectPatchRmsWells = <ThrowOnError extends boolean = false>(options: Options<ProjectPatchRmsWellsData, ThrowOnError>): RequestResult<ProjectPatchRmsWellsResponses, ProjectPatchRmsWellsErrors, ThrowOnError> => (options.client ?? client).patch<ProjectPatchRmsWellsResponses, ProjectPatchRmsWellsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/rms/wells',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List cache revisions
 *
 * Returns a list of revision filenames for the resource specified by the
 * `resource` query parameter. The filenames are used with
 * GET /cache/{revision_id}.
 */
export const projectGetCache = <ThrowOnError extends boolean = false>(options: Options<ProjectGetCacheData, ThrowOnError>): RequestResult<ProjectGetCacheResponses, ProjectGetCacheErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetCacheResponses, ProjectGetCacheErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/cache',
    ...options
});

/**
 * Get cache revision content
 *
 * Retrieve the content of a specific cache revision.
 *
 * The revision_id should be a filename from the list returned by GET /cache
 * (e.g., 20260112T143045.123456Z-a1b2c3d4.json).
 *
 * The `resource` query parameter selects which resource cache to read.
 *
 * Returns the parsed JSON content from the cached file in the `data` field.
 */
export const projectGetCacheRevision = <ThrowOnError extends boolean = false>(options: Options<ProjectGetCacheRevisionData, ThrowOnError>): RequestResult<ProjectGetCacheRevisionResponses, ProjectGetCacheRevisionErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetCacheRevisionResponses, ProjectGetCacheRevisionErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/cache/{revision_id}',
    ...options
});

/**
 * Get diff between current resource and cache revision
 *
 * Compare a resource file in the current project with a cached revision.
 *
 * The `resource` query parameter selects which resource to diff.
 * The response is a list of changes keyed by `field_path`.
 */
export const projectGetCacheDiff = <ThrowOnError extends boolean = false>(options: Options<ProjectGetCacheDiffData, ThrowOnError>): RequestResult<ProjectGetCacheDiffResponses, ProjectGetCacheDiffErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetCacheDiffResponses, ProjectGetCacheDiffErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/cache/diff/{revision_id}',
    ...options
});

/**
 * Restore a resource from a cache revision
 *
 * Restore a resource from a cache revision (overwrites current resource).
 *
 * The current resource state is cached before overwriting (when present).
 *
 * The `resource` query parameter selects which resource to restore.
 *
 * **Example flow:**
 *
 * 1. Current resource is in state A
 * 2. Call restore with a revision from state B
 * 3. Your current state A is cached (when present)
 * 4. Resource is now in state B
 * 5. To undo: restore from the newly created cache entry (your state A backup)
 */
export const projectPostCacheRestore = <ThrowOnError extends boolean = false>(options: Options<ProjectPostCacheRestoreData, ThrowOnError>): RequestResult<ProjectPostCacheRestoreResponses, ProjectPostCacheRestoreErrors, ThrowOnError> => (options.client ?? client).post<ProjectPostCacheRestoreResponses, ProjectPostCacheRestoreErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/cache/restore/{revision_id}',
    ...options
});

/**
 * Checks which missing project .fmu resources can be restored
 *
 * Lists recoverable missing project .fmu content from in-memory state.
 */
export const projectGetRestoreCheck = <ThrowOnError extends boolean = false>(options?: Options<ProjectGetRestoreCheckData, ThrowOnError>): RequestResult<ProjectGetRestoreCheckResponses, ProjectGetRestoreCheckErrors, ThrowOnError> => (options?.client ?? client).get<ProjectGetRestoreCheckResponses, ProjectGetRestoreCheckErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/restore/check',
    ...options
});

/**
 * Restores missing project .fmu resources for the current session
 *
 * Attempts to recover missing project .fmu content from in-memory state.
 */
export const projectPostRestore = <ThrowOnError extends boolean = false>(options?: Options<ProjectPostRestoreData, ThrowOnError>): RequestResult<ProjectPostRestoreResponses, ProjectPostRestoreErrors, ThrowOnError> => (options?.client ?? client).post<ProjectPostRestoreResponses, ProjectPostRestoreErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/restore',
    ...options
});

/**
 * Returns internal mappings for a specific mapping type and source system.
 *
 * Retrieves internal mappings for the specified mapping_type and source_system
 * from the project's .fmu directory.
 *
 * Example: GET /project/mappings/stratigraphy/rms returns the
 * internal stratigraphy mappings whose source system is RMS.
 */
export const projectGetMappings = <ThrowOnError extends boolean = false>(options: Options<ProjectGetMappingsData, ThrowOnError>): RequestResult<ProjectGetMappingsResponses, ProjectGetMappingsErrors, ThrowOnError> => (options.client ?? client).get<ProjectGetMappingsResponses, ProjectGetMappingsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/mappings/{mapping_type}/{source_system}',
    ...options
});

/**
 * Updates internal mappings for a specific mapping type and source system
 *
 * Replaces internal mappings for the specified mapping_type and source system in
 * the project's .fmu directory.
 *
 * The request body should contain the internal mappings collection for the
 * specified source system.
 *
 * Example: PUT /project/mappings/stratigraphy/rms replaces the stored
 * internal stratigraphy mappings whose source system is RMS.
 */
export const projectPutMappings = <ThrowOnError extends boolean = false>(options: Options<ProjectPutMappingsData, ThrowOnError>): RequestResult<ProjectPutMappingsResponses, ProjectPutMappingsErrors, ThrowOnError> => (options.client ?? client).put<ProjectPutMappingsResponses, ProjectPutMappingsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/mappings/{mapping_type}/{source_system}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Imports RMS-to-simulator wellbore mappings from rms_eclipse.csv.
 *
 * Reads an rms_eclipse.csv-format file from the project and returns the
 * imported RMS-to-simulator mappings in the internal wellbore mapping format.
 * The path is relative to the project root. If no path is provided, the default
 * path relative to the project root is
 * rms/input/well_modelling/well_info/rms_eclipse.csv.
 */
export const projectPostMappingsImportRmsEclipseCsv = <ThrowOnError extends boolean = false>(options?: Options<ProjectPostMappingsImportRmsEclipseCsvData, ThrowOnError>): RequestResult<ProjectPostMappingsImportRmsEclipseCsvResponses, ProjectPostMappingsImportRmsEclipseCsvErrors, ThrowOnError> => (options?.client ?? client).post<ProjectPostMappingsImportRmsEclipseCsvResponses, ProjectPostMappingsImportRmsEclipseCsvErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/mappings/import/rms_eclipse_csv',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Exports RMS-to-simulator wellbore mappings to a renaming table.
 *
 * Reads RMS-to-simulator wellbore mappings from mappings.json and writes them
 * to an rms_simulator renaming-table file. The path is relative to the project
 * root. If no path is provided, the default path relative to the project root is
 * rms/input/well_modelling/well_info/rms_simulator.renaming_table.
 */
export const projectPostMappingsExportRmsSimulatorRenamingTable = <ThrowOnError extends boolean = false>(options?: Options<ProjectPostMappingsExportRmsSimulatorRenamingTableData, ThrowOnError>): RequestResult<ProjectPostMappingsExportRmsSimulatorRenamingTableResponses, ProjectPostMappingsExportRmsSimulatorRenamingTableErrors, ThrowOnError> => (options?.client ?? client).post<ProjectPostMappingsExportRmsSimulatorRenamingTableResponses, ProjectPostMappingsExportRmsSimulatorRenamingTableErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/mappings/export/rms_simulator_renaming_table',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Returns changelog for the project.
 *
 * Retrieves changelog from the project's .fmu directory.
 */
export const projectGetChangelog = <ThrowOnError extends boolean = false>(options?: Options<ProjectGetChangelogData, ThrowOnError>): RequestResult<ProjectGetChangelogResponses, ProjectGetChangelogErrors, ThrowOnError> => (options?.client ?? client).get<ProjectGetChangelogResponses, ProjectGetChangelogErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/project/changelog',
    ...options
});

/**
 * Returns the user .fmu configuration
 *
 * The user configuration can store API subscription keys or tokens. These are
 * obfuscated as '**********' when returned.
 */
export const userGetUser = <ThrowOnError extends boolean = false>(options?: Options<UserGetUserData, ThrowOnError>): RequestResult<UserGetUserResponses, UserGetUserErrors, ThrowOnError> => (options?.client ?? client).get<UserGetUserResponses, UserGetUserErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/user/',
    ...options
});

/**
 * Saves an API key/token to the user .fmu configuration
 *
 * Currently only known API's can be saved to the user .fmu configuration.
 * Arbitrary API key-value pairs cannot be saved. The currently known APIs are:
 *
 * smda_subscription
 */
export const userPatchApiKey = <ThrowOnError extends boolean = false>(options: Options<UserPatchApiKeyData, ThrowOnError>): RequestResult<UserPatchApiKeyResponses, UserPatchApiKeyErrors, ThrowOnError> => (options.client ?? client).patch<UserPatchApiKeyResponses, UserPatchApiKeyErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/user/api_key',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Fetches the current session state
 *
 * Retrieves the latest session metadata.
 */
export const sessionGetSession = <ThrowOnError extends boolean = false>(options?: Options<SessionGetSessionData, ThrowOnError>): RequestResult<SessionGetSessionResponses, SessionGetSessionErrors, ThrowOnError> => (options?.client ?? client).get<SessionGetSessionResponses, SessionGetSessionErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/session/',
    ...options
});

/**
 * Creates a new user session.
 *
 * When creating a session the application will ensure that the user
 * .fmu directory exists by creating it if it does not.
 *
 * If a valid session already exists when POSTing to this route, the
 * existing session will be renewed with a new expiry date and a new
 * session cookie value.
 *
 * After creating the session, the application will attempt to find the
 * nearest project .fmu directory above the current working directory and
 * add it to the session if found. If not found, no project will be
 * associated. Renewing an existing session keeps its current state.
 *
 * The session cookie set by this route is required for all other
 * routes. Sessions are not persisted when the API is shut down.
 */
export const sessionPostSession = <ThrowOnError extends boolean = false>(options?: Options<SessionPostSessionData, ThrowOnError>): RequestResult<SessionPostSessionResponses, SessionPostSessionErrors, ThrowOnError> => (options?.client ?? client).post<SessionPostSessionResponses, SessionPostSessionErrors, ThrowOnError>({
    responseType: 'json',
    security: [{ name: 'x-fmu-settings-api', type: 'apiKey' }],
    url: '/api/v1/session/',
    ...options
});

/**
 * Adds a known access token to the session
 *
 * This route should be used to add a scoped access token to the current
 * session. The token applied via this route is typically a dependency for
 * other routes.
 */
export const sessionPatchAccessToken = <ThrowOnError extends boolean = false>(options: Options<SessionPatchAccessTokenData, ThrowOnError>): RequestResult<SessionPatchAccessTokenResponses, SessionPatchAccessTokenErrors, ThrowOnError> => (options.client ?? client).patch<SessionPatchAccessTokenResponses, SessionPatchAccessTokenErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/session/access_token',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Checks which missing user .fmu resources can be restored
 *
 * Lists recoverable missing user .fmu content from in-memory state.
 */
export const sessionGetRestoreCheck = <ThrowOnError extends boolean = false>(options?: Options<SessionGetRestoreCheckData, ThrowOnError>): RequestResult<SessionGetRestoreCheckResponses, SessionGetRestoreCheckErrors, ThrowOnError> => (options?.client ?? client).get<SessionGetRestoreCheckResponses, SessionGetRestoreCheckErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/session/restore/check',
    ...options
});

/**
 * Restores missing user .fmu resources for the current session
 *
 * Attempts to recover missing user .fmu content from in-memory state.
 */
export const sessionPostRestore = <ThrowOnError extends boolean = false>(options?: Options<SessionPostRestoreData, ThrowOnError>): RequestResult<SessionPostRestoreResponses, SessionPostRestoreErrors, ThrowOnError> => (options?.client ?? client).post<SessionPostRestoreResponses, SessionPostRestoreErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/session/restore',
    ...options
});

/**
 * Close the RMS project in the session
 *
 * Close the RMS project that is currently open in the session.
 *
 * This removes the RMS project reference from the session. The project
 * should be closed when it is no longer needed to free up resources.
 */
export const rmsDeleteRmsProject = <ThrowOnError extends boolean = false>(options?: Options<RmsDeleteRmsProjectData, ThrowOnError>): RequestResult<RmsDeleteRmsProjectResponses, RmsDeleteRmsProjectErrors, ThrowOnError> => (options?.client ?? client).delete<RmsDeleteRmsProjectResponses, RmsDeleteRmsProjectErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/rms/',
    ...options
});

/**
 * Open an RMS project and store it in the session
 *
 * Open an RMS project and store it in the session.
 *
 * The RMS project path must be configured in the project's .fmu config file.
 * Once opened, the project remains open in the session until explicitly closed
 * or the session expires. This allows for efficient repeated access without
 * reopening the project each time.
 *
 * The endpoint takes an optional parameter, `rms_version`, as input. This can be
 * used to specify which version of RMS API that should be used when opening
 * the RMS project.
 */
export const rmsPostRmsProject = <ThrowOnError extends boolean = false>(options?: Options<RmsPostRmsProjectData, ThrowOnError>): RequestResult<RmsPostRmsProjectResponses, RmsPostRmsProjectErrors, ThrowOnError> => (options?.client ?? client).post<RmsPostRmsProjectResponses, RmsPostRmsProjectErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/rms/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options?.headers
    }
});

/**
 * Get the zones from the open RMS project
 *
 * Retrieve the zones from the currently open RMS project.
 *
 * This endpoint requires an RMS project to be open in the session.
 * Use the POST / endpoint first to open an RMS project.
 */
export const rmsGetZones = <ThrowOnError extends boolean = false>(options?: Options<RmsGetZonesData, ThrowOnError>): RequestResult<RmsGetZonesResponses, RmsGetZonesErrors, ThrowOnError> => (options?.client ?? client).get<RmsGetZonesResponses, RmsGetZonesErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/rms/zones',
    ...options
});

/**
 * Get all horizons from the open RMS project
 *
 * Retrieve all horizons from the currently open RMS project.
 *
 * This endpoint requires an RMS project to be open in the session.
 * Use the POST / endpoint first to open an RMS project.
 */
export const rmsGetHorizons = <ThrowOnError extends boolean = false>(options?: Options<RmsGetHorizonsData, ThrowOnError>): RequestResult<RmsGetHorizonsResponses, RmsGetHorizonsErrors, ThrowOnError> => (options?.client ?? client).get<RmsGetHorizonsResponses, RmsGetHorizonsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/rms/horizons',
    ...options
});

/**
 * Get all wells from the open RMS project
 *
 * Retrieve all wells from the currently open RMS project.
 *
 * This endpoint requires an RMS project to be open in the session.
 * Use the POST / endpoint first to open an RMS project.
 */
export const rmsGetWells = <ThrowOnError extends boolean = false>(options?: Options<RmsGetWellsData, ThrowOnError>): RequestResult<RmsGetWellsResponses, RmsGetWellsErrors, ThrowOnError> => (options?.client ?? client).get<RmsGetWellsResponses, RmsGetWellsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/rms/wells',
    ...options
});

/**
 * Get the project coordinate system from the open RMS project
 *
 * Retrieve the project coordinate system from the currently open RMS project.
 *
 * This endpoint requires an RMS project to be open in the session.
 * Use the POST / endpoint first to open an RMS project.
 */
export const rmsGetCoordinateSystem = <ThrowOnError extends boolean = false>(options?: Options<RmsGetCoordinateSystemData, ThrowOnError>): RequestResult<RmsGetCoordinateSystemResponses, RmsGetCoordinateSystemErrors, ThrowOnError> => (options?.client ?? client).get<RmsGetCoordinateSystemResponses, RmsGetCoordinateSystemErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/rms/coordinate_system',
    ...options
});

/**
 * Checks whether or not the current session is capable of querying SMDA
 *
 * A route to check whether the client is capable of querying SMDA APIs
 * with their current session. The requirements for querying the SMDA API via
 * this API are:
 *
 * 1. A valid session
 * 2. An SMDA subscription key in the user's .fmu API key configuration
 * 3. A valid SMDA access token scoped to SMDA's user_impersonation scope
 *
 * A successful response from this route indicates that all other routes on the
 * SMDA router can be used.
 */
export const smdaGetHealth = <ThrowOnError extends boolean = false>(options?: Options<SmdaGetHealthData, ThrowOnError>): RequestResult<SmdaGetHealthResponses, SmdaGetHealthErrors, ThrowOnError> => (options?.client ?? client).get<SmdaGetHealthResponses, SmdaGetHealthErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/smda/health',
    ...options
});

/**
 * Searches for a field identifier in SMDA
 *
 * A route to search SMDA for a field (asset) by its named identifier.
 *
 * This endpoint applies a projection to the SMDA query so that only the relevant
 * data is returned: an identifier known by SMDA, its corresponding UUID, and the
 * main country the field belongs to, not a list of countries where the field
 * exists. The UUID should be used by other endpoints required the collection of
 * data by a field, i.e. this route is a dependency for most other routes.
 *
 * The number of results (hits) and number of pages those results span over is also
 * returned in the result. This endpoint does not implement pagination. The
 * current expectation is that a user would refine their search rather than page
 * through different results.
 */
export const smdaPostField = <ThrowOnError extends boolean = false>(options: Options<SmdaPostFieldData, ThrowOnError>): RequestResult<SmdaPostFieldResponses, SmdaPostFieldErrors, ThrowOnError> => (options.client ?? client).post<SmdaPostFieldResponses, SmdaPostFieldErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/smda/field',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Retrieves masterdata for fields to be confirmed in the GUI
 *
 * A route to gather prospective SMDA masterdata relevant to FMU.
 *
 * This route receives a list of valid field names and returns masterdata that
 * pertains to them. The field names should be valid as returned from the
 * `smda/field` route. Callers may also provide a field UUID to disambiguate
 * fields that share the same identifier. If the first field does not include
 * a UUID, the endpoint searches for all provided field identifiers. If the
 * first field includes a UUID, the endpoint looks up only that field, even if
 * more fields are included in the request.
 *
 * The data returned from this endpoint is meant to be confirmed by the user who
 * may need to do some additional selection or pruning based upon the model they
 * are working from.
 *
 * One example of this is changing the coordinate system. A model may use a
 * coordinate system different from the one set as the field's default coordinate
 * system in SMDA. To match the way this works on SMDA, every coordinate system
 * known to SMDA is returned.
 *
 * This endpoint does multiple calls to the SMDA API, any of which may possibly
 * fail. In any of these calls fails incomplete data will _not_ be returned; that
 * is, a successful code with partial data will not be returned.
 */
export const smdaPostMasterdata = <ThrowOnError extends boolean = false>(options: Options<SmdaPostMasterdataData, ThrowOnError>): RequestResult<SmdaPostMasterdataResponses, SmdaPostMasterdataErrors, ThrowOnError> => (options.client ?? client).post<SmdaPostMasterdataResponses, SmdaPostMasterdataErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/smda/masterdata',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Retrieves stratigraphic units for a stratigraphic column
 *
 * A route to gather stratigraphic units from SMDA for a specified
 * stratigraphic column.
 *
 * This route receives a valid stratigraphic column identifier
 * and returns stratigraphic units associated with it. The identifier
 * should be obtained from the `stratigraphic_columns` field in the
 * `smda/masterdata` response.
 */
export const smdaPostStratUnits = <ThrowOnError extends boolean = false>(options: Options<SmdaPostStratUnitsData, ThrowOnError>): RequestResult<SmdaPostStratUnitsResponses, SmdaPostStratUnitsErrors, ThrowOnError> => (options.client ?? client).post<SmdaPostStratUnitsResponses, SmdaPostStratUnitsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/smda/strat_units',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Retrieves well headers for a field
 *
 * A route to gather well header data from SMDA for a specified field.
 *
 * This route receives a valid field identifier and returns the well header
 * fields needed to identify and map SMDA wellbores against model well names.
 */
export const smdaPostWellHeaders = <ThrowOnError extends boolean = false>(options: Options<SmdaPostWellHeadersData, ThrowOnError>): RequestResult<SmdaPostWellHeadersResponses, SmdaPostWellHeadersErrors, ThrowOnError> => (options.client ?? client).post<SmdaPostWellHeadersResponses, SmdaPostWellHeadersErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/smda/well_headers',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Match source names to target names
 *
 * Match source names to target names using strict name similarity.
 *
 * The endpoint is a pure matching utility. Callers provide both the
 * source names and target names, and can optionally provide string
 * replacement rules to apply before matching.
 *
 * Names are normalized before matching by lowercasing, replacing
 * underscores, dots, dashes, and slashes with spaces, collapsing
 * whitespace, and applying optional replacement rules. Replacement
 * rules match whole normalized token sequences only, so a rule like
 * `Fm -> Formation` changes `Tarbert Fm` to `Tarbert Formation`,
 * while `Top -> ""` leaves `Stop Viking` unchanged.
 *
 * The response contains one result per source. Each result contains up
 * to three target matches, ordered from highest to lowest score.
 *
 * Confidence levels:
 * - 'high': score > 80
 * - 'medium': score 50-80
 * - 'low': score < 50
 */
export const matchPostMatch = <ThrowOnError extends boolean = false>(options: Options<MatchPostMatchData, ThrowOnError>): RequestResult<MatchPostMatchResponses, MatchPostMatchErrors, ThrowOnError> => (options.client ?? client).post<MatchPostMatchResponses, MatchPostMatchErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/match',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * A health check on the /v1 routes.
 *
 * This route requires a valid session to return 200 OK. it can used to check if the user has a valid session.
 */
export const healthV1HealthCheck = <ThrowOnError extends boolean = false>(options?: Options<HealthV1HealthCheckData, ThrowOnError>): RequestResult<HealthV1HealthCheckResponses, HealthV1HealthCheckErrors, ThrowOnError> => (options?.client ?? client).get<HealthV1HealthCheckResponses, HealthV1HealthCheckErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/v1/health',
    ...options
});

/**
 * A health check on the application
 *
 * This route requires no form of authentication or authorization. It can be used to check if the application is running and responsive.
 */
export const healthHealthCheck = <ThrowOnError extends boolean = false>(options?: Options<HealthHealthCheckData, ThrowOnError>): RequestResult<HealthHealthCheckResponses, unknown, ThrowOnError> => (options?.client ?? client).get<HealthHealthCheckResponses, unknown, ThrowOnError>({
    responseType: 'json',
    url: '/health',
    ...options
});
